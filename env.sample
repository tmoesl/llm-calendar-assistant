# =============================================================================
# Complete Configuration Template
# =============================================================================
# Copy this file to .env and configure the required values
#
# 🏷️ Tags: (Infrastructure) = Docker / Makefile
# 🏷️ Tags: (App) = Python application runtime

# ✅ QUICK START (Set these required values)
# DATABASE_HOST, DATABASE_PASSWORD, DATABASE_USER
# OPENAI_API_KEY, ANTHROPIC_API_KEY

# ℹ️ NOTE: API, Redis, and Flower ports refer to external host ports.
# Internal container ports are hardcoded (API: 8080, Redis: 6379, Flower: 5555).

# =============================================================================
# PROJECT CONFIGURATION
# =============================================================================
# 📌 Must Exist
PROJECT_NAME=llm-calendar-assistant

# =============================================================================
# API CONFIGURATION (Infrastructure)
# =============================================================================
# 📌 Must Exist
API_PORT=8080

# =============================================================================
# WORKER CONFIGURATION (App + Infrastructure)
# =============================================================================
# 📌 Must Exist
FLOWER_HOST=localhost
FLOWER_PORT=5555

# 💡 Optional
CELERY_CONCURRENCY=4
CELERY_LOG_LEVEL=DEBUG

# =============================================================================
# REDIS CONFIGURATION (App + Infrastructure)
# =============================================================================
# 📌 Must Exist
REDIS_HOST=localhost
REDIS_PORT=6379

# =============================================================================
# DATABASE CONFIGURATION (App)
# =============================================================================
# 🔧 Must Set
DATABASE_HOST=your_supabase_host.supabase.com
DATABASE_PASSWORD=your_supabase_password
DATABASE_USER=postgres.your_project_id

# 💡 Optional
DATABASE_NAME=postgres
DATABASE_PORT=6543

# =============================================================================
# CALENDAR CONFIGURATION (App) 
# =============================================================================
# 💡 Optional - Google Calendar Settings
CALENDAR_ID=primary
CALENDAR_USER_TIMEZONE=Australia/Sydney
CALENDAR_TOKEN_PATH=token.json
CALENDAR_CREDENTIALS_PATH=credentials.json
CALENDAR_API_NAME=calendar
CALENDAR_API_VERSION=v3
CALENDAR_SCOPES=["https://www.googleapis.com/auth/calendar"]

# =============================================================================
# LLM PROVIDERS (App)
# =============================================================================
# 🔧 Must Set
OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# 💡 Optional - OpenAI Settings
OPENAI_MODEL=gpt-4o
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_TEMPERATURE=0.0
OPENAI_MAX_TOKENS=2048
OPENAI_MAX_RETRIES=3
OPENAI_TIMEOUT=30

# 💡 Optional - Anthropic Settings
ANTHROPIC_MODEL=claude-sonnet-4-20250514
ANTHROPIC_TEMPERATURE=0.0
ANTHROPIC_MAX_TOKENS=2048
ANTHROPIC_MAX_RETRIES=3
ANTHROPIC_TIMEOUT=30

# 💡 Optional - LLM Processing Settings
LLM_CONFIDENCE_THRESHOLD=0.7

# =============================================================================
# LOGGING CONFIGURATION (App)
# =============================================================================
# 💡 Optional
LOG_LEVEL=DEBUG
LOG_CONSOLE_OUTPUT=true
LOG_FILE_OUTPUT=true

# =============================================================================
# 📝 CONFIGURATION APPROACH
# =============================================================================
# 🔧 MUST SET: Requires customization (e.g., secrets, keys)
# 📌 MUST EXIST: Standard values, no codebase defaults
# 💡 OPTIONAL: Has codebase defaults – override if needed
#
# 🐳 DOCKER: Uses .env + .env.docker for container-specific overrides 